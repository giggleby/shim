board: CHROMEBOOK


##### BEGIN CHECKSUM BLOCK
#
# WARNING: This checksum is generated and audited by Google. Do not
# modify it. If you modify it, devices' configurations will be
# invalid, and the devices may not be sold.
#
# 警告：此校验码由 Google 产生及审核，禁止手动修改。
# 若修改将使设备配置變為无效，并且不得销售此设备。
#
#####
checksum:

##### END CHECKSUM BLOCK. See the warning above. 请参考上面的警告。


encoding_patterns:
  0: default

image_id:
  0: TEST

pattern:
  - image_ids: [0]
    encoding_scheme: base8192
    fields:
    - zero_bit_comp_field: 0
    - display_panel_field: 1

encoded_fields:
  zero_bit_comp_field:
    0: { zero_bit_comp: zero_bit_comp_0 }
  display_panel_field:
    0: { display_panel: display_panel_0 }
    1: { display_panel: display_panel_1 }

components:
  zero_bit_comp:
    items:
      zero_bit_comp_0:
        values:
          attr1: AAA
  display_panel:
    items:
      display_panel_0:
        values:
          the_attribute: AAA
      display_panel_1:
        values:
          the_attribute: BBB
      display_panel_invalid1:
        values:
          the_attribute: CCC
      display_panel_invalid2:
        values:
          the_attribute: DDD
      display_panel_123_456:  # Inserting (not appending) a new component is
                              # often problemetic.
        status: unqualified
        values:
          the_attribute: FFF
      display_panel_100_200:
        values:
          the_attribute: EEE

rules:
- name: device_info.set_image_id
  evaluate: SetImageId('TEST');
