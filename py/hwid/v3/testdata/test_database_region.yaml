checksum:

project: CHROMEBOOK

encoding_patterns:
  0: default

image_id:
  0: AAA

pattern:
  - image_ids: [0]
    encoding_scheme: base8192
    fields: []

encoded_fields:
  region_field: !region_field [us, gb, jp]


components:
  region: !region_component

rules: []
